[build]
  base = "frontend"
  publish = "dist"
  command = "npm ci && npm run build"

[build.environment]
  # Keep Netlify's build environment aligned with GitHub Actions (Node 20.x)
  NODE_VERSION = "20"

